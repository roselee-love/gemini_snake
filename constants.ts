// Grid Dimensions (3x3)
export const GRID_SIZE = 9;

// Game Config
export const GAME_DURATION = 30; // Seconds
export const MOLE_STAY_DURATION = 800; // ms a mole stays visible
export const MOLE_SPAWN_INTERVAL = 600; // ms between spawn attempts

// Initial State
export const INITIAL_MOLES = Array(GRID_SIZE).fill(false);
